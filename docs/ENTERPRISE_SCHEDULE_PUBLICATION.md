# 企业排班发布

`POST /api/enterprise/workforce/schedule-publication` 在班表发布前检查岗位覆盖、岗位资质、休息规则、加班审批、员工可用性、劳动规则、班次冲突、薪资映射和审计证据。

- `PUBLISH`：用工与控制条件完整，可以发布排班。
- `REBALANCE`：不存在合规阻断，但仍需调整覆盖、审批、冲突或通知准备。
- `BLOCKED`：资质、休息规则、劳动规则、薪资映射或审计控制失败。

接口可向员工端、考勤终端和薪资系统输出一致的排班发布结论。
